pytris

Simple Tetris clone in PyGame.

Run:

  $ . venv/bin/activate
  (venv) $ python3 pytris.ps


Code:

  $ git clone XXX
  $ brew install sdl sdl_image sdl_mixer sdl_ttf portmidi
  $ python3 -m venv venv
  $ . venv/bin/activate
  (venv) $ pip3 install -r requirements.txt

